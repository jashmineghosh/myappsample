import scala.io.StdIn.{readLine}


object new1 {
  def  main(args: Array[String]): Unit = {
    println("hello")
    //def sum (n:Int):Int = {if (n<1) 0 else 
    //readInt() + sum(n-1)}
    //println(s"sums is sum(_)")
    val input = readLine.toInt
    println("this is $(input):")
  }
}